package model.entity.enemies;

public class EntityStateManager {

    // Cambia lo stato del nemico a IDLE
//enemy.setState(EnemyState.IDLE);

// Cambia lo stato del nemico a MOVEMENT
//enemy.setState(EnemyState.MOVEMENT);

// Cambia lo stato del nemico a ATTACK
//enemy.setState(EnemyState.ATTACK);
}
